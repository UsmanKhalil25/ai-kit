# AI Kit

Reusable OpenCode configurations and workflows.

## Usage

Copy a package to your project's `.opencode/` directory:

```bash
cp -r opencode/<package>/* /path/to/your/project/.opencode/
```

See individual package READMEs for setup instructions.

